// 배포 버전. **push 전에 `python bump.py <새값>` 으로 올린다** (CLAUDE.md 규칙 3).
//
// 이 값은 화면 오른쪽 아래에 표시되고, 모든 import 경로의 ?v= 와 같아야 한다.
// 아이패드에서 이 표시가 안 바뀌면 캐시가 안 뚫린 것이고,
// 바뀌었는데 동작이 그대로면 진짜 코드 문제다. 둘이 구분된다는 게 요점이다.
export const VERSION = '20260727a';
