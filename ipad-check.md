# 아이패드 실측 — 미룬 확인

주소: <https://rukihana.github.io/relationship_map/>

> 계획서 §10 은 배포를 1단계 안에 넣으면서 「**아이패드에서 실제로 어떻게 도는지가
> 이 설계의 유일한 미검증 부분이다. 기능을 더 쌓기 전에 확인한다**」고 적었다.
> 실제로는 2단계까지 쌓인 채로 미뤘다. **그래서 여기서 문제가 나오면 1단계 것인지
> 2단계 것인지 가려야 한다** — 아래 표의 「어디 것인가」 칸이 그걸 위한 것이다.

확인한 항목은 `[ ]` 를 `[x]` 로 바꾸고, 안 되는 게 있으면 그 줄 아래에 증상을 적는다.

---

## A. 터치 전용 경로 — 여기가 제일 중요하다

데스크톱에서는 재현할 방법이 없어서 **한 번도 확인 못 한 것들**이다.

| | 확인 | 어디 것인가 | 안 되면 무슨 뜻인가 |
|---|---|---|---|
| [ ] A1 | `연결` 버튼을 켜고 **두 노드를 차례로 탭** → 역할 선택창이 뜬다 | 2단계 | 아이패드에서 관계를 만들 방법이 없어진다. 연결점은 터치 기기에 일부러 안 띄운다 |
| [ ] A2 | 노드를 **탭 한 번**에 고정되고 관계 카드가 뜬다 | 1단계 | 호버가 없으므로 이게 아이패드의 **유일한 진입**이다 |
| [ ] A3 | 고정 상태에서 **관계선을 손가락으로** 누를 수 있다 | 1단계 | 못 눌러도 카드가 안전망이라 치명적이진 않다(§7 ④) |
| [ ] A4 | 노드를 끌어 옮기고, **손을 뗐을 때 따라다니지 않는다** | 1단계 | `pointercancel` 처리가 안 먹는 것. §7 이 콕 집어 경고한 증상 |
| [ ] A5 | 두 손가락으로 확대·축소된다 | 1단계 | |
| [ ] A6 | 분할선을 손가락으로 집어 위아래 비율이 바뀐다 | 1단계 | 9px 시각 + 히트영역 27px 로 넓혀뒀다(§7-1). 안 잡히면 더 넓혀야 한다 |
| [ ] A7 | `▲` / `▼` 접기 버튼이 손가락으로 눌린다 | 1단계 | 분할선을 못 집는 경우의 대비책이다 |

## B. 사파리 고유 동작 — 안 막으면 데이터가 날아간다

| | 확인 | 안 되면 무슨 뜻인가 |
|---|---|---|
| [ ] B1 | **위에서 아래로 끌어도 새로고침되지 않는다** | 새로고침되면 **되돌리기 기록이 통째로 날아간다**(§7). `overscroll-behavior: contain` 이 안 먹은 것 |
| [ ] B2 | 화면 가장자리에서 시작한 끌기가 뒤로가기로 먹히지 않는다 | 관계도를 가장자리에 안 붙여뒀는데도 먹으면 여백을 더 줘야 한다 |
| [ ] B3 | 페이지 자체가 확대되지 않는다 (확대는 관계도만) | `maximum-scale=1` 이 안 먹은 것 |
| [ ] B4 | **한글을 빠르게 이어 칠 때 글자가 먹지 않는다** | 조합 처리 문제. 2층 중 글자를 `textarea` 가 그리게 뒤집은 게 이걸 막으려는 것이었다 |
| [ ] B5 | 하이라이트(오류 줄 배경)가 실제 줄과 **어긋나지 않는다** — 길게 스크롤한 상태에서도, **한글 입력 중에도** | 두 층의 줄바꿈이 어긋난 것 |

## C. 저장과 입출구

| | 확인 | 안 되면 무슨 뜻인가 |
|---|---|---|
| [ ] C1 | `데이터 내보내기` 가 **파일 앱에 저장**된다 | 안 되면 클립보드 3단 대체로 물러선다(§8). 어디까지 물러섰는지 봐야 한다 |
| [ ] C2 | 그 파일을 `파일 열기` 로 다시 넣을 수 있다 | 왕복이 성립하는지 |
| [ ] C3 | 앱을 닫았다 열면 **「지난 작업 이어하기」** 가 뜬다 | `localStorage` 가 사파리에서 사는지 |
| [ ] C4 | **홈화면에 추가**한 뒤 아이콘으로 열린다 | |
| [ ] C5 | ⚠ 홈화면 앱과 사파리의 저장이 **실제로 갈라지는지** 눈으로 확인 | §1 이 「입구를 하나로 통일한다」고 정한 이유를 직접 봐두는 것. 확인했으면 **그 뒤로는 사파리로 이 주소를 열지 않는다** |

## D. 화면

| | 확인 | 안 되면 무슨 뜻인가 |
|---|---|---|
| [x] D1 | ~~**30명을 넣었을 때 노드가 서로 밟지 않는다**~~ — **2026-07-26 실측 완료**(아래) | 브라우저에서 재봤다. 아이패드 실기기에서는 글자가 실제로 읽히는지만 눈으로 확인하면 된다 |
| [ ] D2 | 역할 선택창이 손가락으로 쓸 만하다 — 검색칸 · 계열 접기 · 항목 버튼 크기 | **데스크톱 크기로만 짰다.** 여기가 제일 의심스럽다 |
| [ ] D3 | 관계 카드의 `고치기`/`지우기`/`시트` 가 눌린다 | 터치 기기에서는 늘 보이게 해뒀다 |
| [ ] D4 | 위아래 비율이 다음에 열었을 때 유지된다 | |
| [ ] D5 | 캐릭터 시트에서 소속·색·항목·메모를 고칠 수 있다. **항목 순서 바꾸는 ↑↓ 가 눌린다** | 3단계 |
| [ ] D6 | 관계도 도구가 7개로 늘어 줄바꿈되는데 손가락으로 다 눌린다 | `연결`·`전체 보기`·`정리`·`범례`·`SVG`·`맞춤`·`▲` |

## F. 3·4·5단계에서 늘어난 것

| | 확인 | 안 되면 무슨 뜻인가 |
|---|---|---|
| [ ] F1 | `정리` 를 누르면 노드가 흩어지고, **되돌리기 한 번으로 원래 자리로 온다** | |
| [ ] F2 | `범례` 가 열리고 줄을 눌러 계열 선을 감출 수 있다 | |
| [ ] F3 | 위 검색칸에 이름을 치면 그 노드만 또렷해진다. **초성(`ㅈㄴ`)도 된다** | 아이패드 한글 키보드에서 초성만 입력하는 게 되는지도 같이 본다 |
| [ ] F4 | `SVG` 를 누르면 파일 앱에 저장된다 | 사파리가 SVG 내려받기를 어떻게 다루는지 미확인 |
| [ ] F5 | `저장` 단추가 **안 보인다** | 사파리에는 없는 기능이라 숨는 게 정상이다(§8) |
| [ ] F6 | 홈화면에 추가한 뒤 **비행기 모드로 열린다** | 서비스워커. 한 번 방문해 둬야 받는다 |
| [ ] F7 | 새 배포가 있으면 「새 버전이 준비됐습니다」 안내가 뜬다 | 조용히 갈아치우지 않는다 — 새로고침되면 되돌리기 기록이 날아간다 |

## E. 캐시 — 이건 다음 배포 때 한 번만

| | 확인 |
|---|---|
| [ ] E1 | 눈에 보이는 것 하나를 고치고 `python bump.py` → 커밋 → push. 아이패드에서 **새로고침 없이** 앱을 다시 열어, 오른쪽 아래 버전 표시와 고친 내용이 **같이** 바뀌는지 |

버전만 바뀌고 내용이 그대로면 캐시가 아니라 코드 문제다. **둘이 구분된다는 게 요점이다**(§3).

---

## 30명 밀도 — 2026-07-26 실측 (브라우저)

인물 30명 · 관계 40건을 넣고 `정리` 를 누른 뒤 노드 상자끼리의 겹침을 쟀다.

| 화면 | 관계도 크기 | 배율 | 글자 | 겹침 |
|---|---|---|---|---|
| 아이패드 가로 · 기본 65:35 | 1024×391 | 0.91 | 10.9px | **1쌍** |
| 아이패드 가로 · 목록 접음 | 1024×567 | 0.94 | 11.3px | 0 |
| 아이패드 세로 · 기본 65:35 | 768×557 | 0.94 | 11.2px | 0 |
| 아이패드 세로 · 목록 접음 | 768×823 | 1.11 | 13.4px | 0 |

**밟히는 건 아이패드 가로에서 목록을 펼친 채로 둘 때 하나뿐이다.** 그때는 `정리` 가 경고를 띄우고 「목록 접고 다시 정리」 단추를 같이 준다.

⚠️ **이 측정에서 나온 것들** (전부 고쳤다)
- `정리` 가 화면 크기를 몰라 넓게 퍼졌고, `맞춤` 이 그걸 담으려고 **배율을 0.46 까지 줄여 글자가 5.5px** 이 됐다. 겹치지는 않는데 못 읽는 상태였다 → 화면을 경계로 삼아 시뮬레이션을 가두게 고쳤다
- **접기가 아예 안 먹었다.** `collapsed` 클래스는 붙는데 인라인 `flex: 35 1 0%` 가 이겼다. 접기 단추도 분할선 끝까지 밀기도 둘 다 무효였다 → 인라인을 같이 고치게 했다. 접혀도 손잡이 높이(34px)는 남긴다(§7)

**실기기에서 볼 것**: 위 배율에서 한글이 실제로 읽히는지. 10.9px 는 화면상 숫자이고 아이패드 해상도에서 어떻게 보이는지는 눈으로 봐야 한다.

## 지금 배포된 것

| | |
|---|---|
| 버전 | 오른쪽 아래 표시로 확인 (`bump.py` 가 올린 값) |
| 1단계 | 텍스트 목록 · 파싱 · 되돌리기 · 노드 배치 · 단계적 노출 · 관계 카드 · 가져오기/내보내기 |
| 2단계 | 연결 제스처 · 역할 선택창 · 줄 단위 교체 · `split.js`/`bundle.js` |
| 3단계 | 캐릭터 시트 · `이 캐릭터만 복사` · 크롬 `저장` · 임시 어휘 관리 |
| 4단계 | `정리` · 범례와 필터 · 초성 검색 · 관계도 SVG · 오프라인(서비스워커) |
| 5단계 | 단축키 · 파서 모호성 해결 창 · 인쇄용 스타일 |
