# charmap — 캐릭터 관계도 웹앱

바닐라 JS + SVG + ES 모듈. 프레임워크·빌드·npm 없음. **저장소가 곧 사이트다** (`dist/`도 `build.js`도 없다).

| | |
|---|---|
| 배포 주소 | <https://rukihana.github.io/relationship_map/> |
| 저장소 | `RukiHana/relationship_map` · 공개 · Pages 는 `main` 브랜치 루트(`/`) |
| 갱신 | `python bump.py` → 커밋 → `git push`. 빌드 단계 없음 |

**배포 주소와 `localhost:8000` 은 브라우저 입장에서 다른 사이트다.** 저장이 따로 놀아서 개발 중 작업은 배포본과 별개로 쌓인다. 주소를 바꾸면(저장소 이름 변경·커스텀 도메인) 그 전 저장분은 새 주소에서 안 보인다 — 파일이 원본이라 잃는 건 없지만, 바꿀 거면 **내보내기를 먼저** 한다.

원본 명세서는 저장소 **바깥**에 있다 — `D:\relationship map\charmap-plan.md`. 캐릭터 이름과 설정이 들어 있어 커밋하지 않는다. 아래 규칙의 근거는 전부 그 문서의 절 번호로 적는다.

---

## 절대 규칙 — 어기면 되돌리기 어렵다

### 1. `data/` 와 `charmap*.json` 은 커밋하지 않는다 (§1)

저장소가 공개이고 **깃은 지운 파일도 히스토리에 남긴다.** 한 번 커밋되면 다음 커밋에서 지워도 계속 꺼내볼 수 있고, 되돌리려면 히스토리를 다시 써야 한다.

커밋 전마다:

```bash
git ls-files
```

**통째로 눈으로 훑는다.** `Select-String data` 로 거르지 않는다 — 루트에 떨어진 `charmap-20260726-1840.json` 은 이름에 `data`가 없어서 그 방식으로는 안 걸리는데, 그게 제일 위험한 파일이다. 파일이 30개 남짓이라 눈으로 보는 게 더 빠르고 확실하다.

### 2. 앱은 `data/` 를 읽지 않는다 (§1)

개발 중에는 로컬 서버가 저장소 폴더를 서빙하니 `fetch('data/charmap.json')` 이 **실제로 성공한다.** 하지만 배포본에는 `data/`가 없다. 그 코드는 내 컴퓨터에서만 되고 아이패드에서 깨진다.

`data/`는 사람과 `split.js`/`bundle.js`만 쓰는 폴더다. 앱에 데이터가 들어가는 경로는 둘뿐 — **브라우저에 저장돼 있던 것**, 또는 **사용자가 파일을 고르거나 번들을 붙여넣은 것.**

```bash
python -c "import pathlib;[print(p,':',l) for p in pathlib.Path('src').rglob('*') if p.is_file() for l in p.read_text(encoding='utf-8').splitlines() if 'data/' in l]"
```

결과가 비어 있어야 한다.

### 3. push 전에 버전 값을 올린다 (§3)

ES 모듈은 `import`로 이어진 파일이 각각 따로 캐시된다. `index.html`이 새로 와도 `src/*.js`는 옛것이 온다. 아이패드 사파리에는 강력 새로고침이 없다.

```bash
python bump.py 20260727a
```

`index.html`·`src/*.js`의 모든 `?v=` 와 `src/version.js`의 `VERSION`을 한 번에 바꾼다. 화면 우하단 표시도 같은 값이다 — **아이패드에서 그 표시가 안 바뀌면 캐시가 안 뚫린 것이고, 바뀌었는데 동작이 그대로면 진짜 코드 문제다. 둘이 구분된다는 게 요점이다.**

### 4. `roles.json` 에 세계관 고유명이 든 어휘를 넣지 않는다 (§5)

`src/roles.json` 은 **커밋되고 공개된다.** 규칙 1의 그물 밖에 있는 유일한 구멍이다.

| | 예 | 어디에 |
|---|---|---|
| 층위1·2 | 엄마·딸·스승·짝사랑·원수·룸메이트 | `src/roles.json`. 커밋한다 |
| 층위3 중 장르 일반어 | 군주·길드장·사부·영주 | 커밋해도 된다. 작품이 안 드러난다 |
| 층위3 중 **세계관 고유명이 박힌 것** | 특정 가문·문파·조직 이름이 든 직위와 호칭 | **커밋하지 않는다.** 번들 안의 `roles`로만 산다 |

판별 기준은 관계 사전과 같다 — **「그 관계에 세계관·조직 고유명이 붙어야 성립하나?」**

커밋 전에 `src/roles.json` 을 한 번 읽어 작품 고유 어휘가 섞이지 않았는지 본다.

### 5. 한 번 쓰인 관계명(`label`)은 고치지 않는다 — 추가만 (§5)

`엄마`를 `모친`으로 고치면 이미 적힌 `지나-당옥 : 딸 - 엄마` 가 전부 모르는 역할이 되어 회색 선으로 떨어진다. **에러는 안 난다** — 파싱은 파괴적이지 않아 줄은 그대로 남고 색만 조용히 빠진다. 며칠 뒤에 발견한다.

- 뜻을 다듬고 싶으면 `note`를 고친다
- 말을 바꿔야겠으면 **새 항목을 추가하고 옛 이름을 새 항목의 `aliases`에 넣는다**
- **삭제도 같다.** 배열에서 지우지 말고 `hidden: true`로 감춘다

### 6. 이름은 유일하다 (§4)

관계 줄이 이름으로 적히므로 같은 이름이 둘이면 `지나-당옥 : 딸 - 엄마` 가 **어느 지나인지 정할 방법이 없다.** 파서를 잘 짜서 풀 문제가 아니라 형식 자체에 답이 없다.

- 캐릭터 추가·이름 변경에서 기존 이름과 같으면 **거절**한다
- 비교는 NFC 정규화 + 앞뒤 공백 제거 후
- **가져오기만 예외** — 겹치면 막지 말고 뒤엣것에 `-2`를 붙이고 알린다. 통째로 거절하면 고칠 방법이 없어진다

---

## 파일이 뭘 맡나 (§3)

```
.gitignore      data/ 와 charmap*.json — 첫 커밋 전에 적었다
.nojekyll       깃헙 페이지가 파일을 그대로 내보내게 함
robots.txt      검색 색인 차단 (index.html의 noindex와 한 쌍)
index.html      진입점. <script type="module" src="src/main.js?v=...">
bump.py         버전 값 일괄 갱신 (규칙 3)
split.js        charmap-*.json 최신 → data/characters/*.json + relations.md   [2단계]
bundle.js       그 반대                                                        [2단계]

src/
  style.css     시안 1a 토큰
  version.js    VERSION 상수 하나. bump.py가 고친다
  roles.json    관계 어휘 — 계열·층위·짝·수식 (규칙 4·5)
  parse.js      텍스트 → 관계 + 진단.  ★ import 하지 않는다 (아래)
  serialize.js  관계 → 줄 · 줄 단위 교체 · 번들 묶기/풀기.  ★ import 하지 않는다
  state.js      상태 · mutate() · 되돌리기 · 다시그리기 예약
  model.js      캐릭터 CRUD · 인접목록 · id 발번 · NFC
  roles.js      어휘 조회 · 역방향 · 계열 색 · 3원 병합
  ui_text.js    텍스트 목록 (하이라이트 pre + 투명 textarea 2층)
  ui_graph.js   SVG · 포인터 제스처 · 단계적 노출 · 선 최근접 판정
  ui_card.js    관계 카드 (읽기 전용) · 선 ↔ 줄 양방향
  ui_dialog.js  확인·입력·체크목록·안내 줄  ← 계획서 목록에 없던 파일. 삭제 미리보기·
                가져오기 비교·어휘 추가·선 후보가 전부 같은 물건을 필요로 해서 모았다
  ui_sheet.js   캐릭터 시트 폼                                                 [3단계]
  io.js         가져오기/내보내기 · 임시 보관 · 스냅샷 · 클립보드 3단

tests/
  cases.js         케이스 원본. 두 러너가 공유한다
  parse.test.js    node 러너   →  node tests/parse.test.js
  parse.test.html  브라우저 러너 → localhost:8000/tests/parse.test.html

data/           ← 커밋하지 않음. 앱도 읽지 않음
```

### `parse.js` 와 `serialize.js` 는 아무것도 import 하지 않는다

규칙 3의 `?v=` 를 붙이면 node ESM이 `./roles.js?v=20260726a` 를 해석하지 못해 `node tests/parse.test.js` 가 통째로 못 돈다. 그래서 이 둘은 **아는 이름 목록과 어휘 인덱스를 인자로 받는 순수 모듈**이다.

NFC 정규화 같은 공용 함수는 `parse.js` 가 export 하고 다른 모듈이 거기서 가져다 쓴다. 정의는 한 군데뿐이고 역방향 의존이 안 생긴다.

---

## 관계 줄 형식 (§4, §6)

```
지나-당옥 : 딸 - 엄마
지나-선 : 짝사랑 -
지나-달래 : 딸 - 엄마(의붓)
"장-보고"-당옥 : 부하 - 상관

# 2부에서 뒤집힘
```

읽는 법은 **「A는 B의 roleA이고, B는 A의 roleB이다」**.

- **한쪽 관계는 역할 한쪽을 비운다.** 빈 쪽은 유효한 값이다 — 이름 쪽이 비면 오류
- **수식은 괄호로.** 폐쇄목록 6개(과거·예정·의붓·양자·부계·모계)뿐. 쉼표로 여러 개 — `할머니(부계, 과거)`
- **관계명 자체에는 괄호를 쓰지 않는다.** 수식으로 오독된다
- 이름에 `-`·`:`·괄호가 있으면 앱이 되쓸 때 따옴표로 감싼다. 따옴표 안의 `-`와 `:`는 구분자가 아니다
- **빈 줄과 `#` 주석은 그대로 보존한다**
- 같은 두 사람 사이에 관계가 여러 개일 수 있다. 관계는 목록이지 쌍으로 키를 매긴 사전이 아니다

### 파싱은 파괴적이지 않다 (§6)

못 알아본 줄은 **적힌 그대로 남기고 옆에 표시만 한다.** 사용자가 쓴 줄을 지우거나 몰래 고쳐 쓰지 않는다. 모르는 역할, 폐쇄목록 밖 수식, 자기 자신과의 관계, 똑같은 줄 두 번, 짝이 안 맞는 관계 — 전부 **표시만 하고 막지 않는다.**

`parse(serialize(parse(x))) === parse(x)` 왕복 테스트가 이걸 지킨다.

### 두 화면의 권한 규칙 (§4)

> 관계 내용의 원본은 **텍스트**다. 관계도에서 편집하면 해당 **줄 하나만 갈아끼운다.** 문서 전체를 다시 써내는 일은 없다.

이걸 안 지키면 두 화면이 서로 덮어쓰기 시작한다.

### 텍스트 동기 방향

- `textarea` 는 **한 번 만들고 절대 다시 만들지 않는다.** 다시 그리면 한글 조합 중에 글자가 먹는다
- **타이핑 → 상태**: 디바운스 파싱. 조합 중에는 미루되, 조합 이벤트가 안 오는 경우 대비로 입력이 멎으면 조합 여부와 무관하게 파싱하는 타이머를 같이 둔다. 이 경로에서 앱은 `textarea.value` 를 **절대 건드리지 않는다**
- **상태 → 텍스트**: 가져오기·삭제·이름변경 셋뿐. 전부 사용자가 확인을 누른 직후라 조합 중일 수 없다
- 하이라이트 `pre` 레이어는 입력이 없는 층이라 **언제든 다시 그려도 안전하다**

### 2층 중 어느 쪽이 글자를 그리나 — 계획서를 한 겹 뒤집었다

계획서 §7-1 은 「`pre` 로 색칠하고 그 위에 글자를 투명하게 만든 `textarea` 를 포갠다」로 적었다. 두 층을 두는 이유(「`textarea` 는 특정 줄만 색칠할 수 없다」)는 그대로지만, **글자는 `textarea` 가 그리고 `pre` 는 배경만 칠하게** 뒤집었다.

한글을 조합하는 동안 미확정 글자는 `textarea` 안에만 있고 `pre` 에는 없다. 글자를 `pre` 가 그리면 조합 중인 글자가 사라지거나 두 층이 어긋난다 — §11 이 「한글 입력 중에도 어긋나지 않는지」를 확인 항목으로 둔 바로 그 증상이다. 배경만 칠하면 조합 중에도 절대 안 어긋난다. 잃는 것은 줄 안의 특정 낱말만 색칠하는 것인데, 필요한 강조가 전부 **줄 단위**라 실제로 잃는 게 없다.

### `commit()` 은 사용자가 실제로 친 경우에만 반영한다

「원본은 텍스트」(§4)는 **「사용자가 텍스트에 쓴 것이 원본」**이라는 뜻이지 「DOM 노드의 현재 값이 무조건 이긴다」는 뜻이 아니다.

이 구분이 없으면 이런 사고가 난다 — 탭이 뒤에 있어 다시그리기가 밀리는 동안 `textarea` 는 아직 상태와 동기되기 전(빈 값)인데, 그 상태로 페이지를 떠나면 `pagehide` → `flushText` → `commit` 이 **빈 값을 원본으로 믿고 관계 줄을 통째로 지운다.** 개발 중에 실제로 12줄이 이렇게 날아갔다. `ui_text.js` 의 `userTyped` 플래그가 그 가드이고, `render()` 쪽에도 거울이 되는 가드가 같이 있다(반영 안 된 타이핑이 있으면 덮지 않는다).

---

## 화면 (§7, §7-1)

**디자인은 시안 1a 「미니멀 실무형」.** 목업은 `D:\relationship map\` 에 있고 **저장소로 옮기지 않는다** (캐릭터 이름 10명이 들어 있다). 값만 `src/style.css` 로 옮겼다.

### 기본 화면은 노드만 — 선은 고르면 나온다

| 단계 | 무엇을 하면 | 무엇이 보이나 |
|---|---|---|
| 0 · 쉼 | — | **노드만.** 선도 라벨도 없다 |
| 1 · 훑기 | 노드에 마우스를 올리면 | 그 인물의 **선만**. **관계명은 안 나온다** |
| 2 · 고정 | 노드를 클릭 | 선 고정 + **관계 카드**. 빈 곳을 눌러 푼다 |
| 3 · 확인 | 고정 상태에서 선을 클릭 | 그 관계의 텍스트 |

1단계에서 관계명을 감추는 게 핵심이다 — **「누구와 엮였나」와 「무슨 사이인가」를 분리한다.**

**아이패드에는 1단계가 없다.** 터치에 호버가 없어 탭하면 곧바로 2단계로 간다. **길게 누르기는 쓰지 않는다** (브라우저 자체 메뉴와 충돌한다).

`전체 보기` 토글을 켜면 선과 라벨이 전부 나온다. 그 상태가 시안 1a가 보여주는 화면이고, SVG 내보내기도 그 상태를 뽑는다.

관계가 0인 노드는 테두리가 점선이다.

### 아이패드에서 반드시 필요한 것

`touch-action: none` · 페이지 자체 확대 차단 · `pointercancel` 처리(빼면 손을 뗐는데도 노드가 따라다닌다) · `overscroll-behavior: contain`(당겨서 새로고침되면 **되돌리기 기록이 통째로 날아간다**) · 관계도를 화면 가장자리에 붙이지 않기(사파리 가장자리 끌기가 뒤로가기로 먹힌다).

### 좌표는 픽셀이다

목업의 `%` + `preserveAspectRatio="none"` 은 쓰지 않는다 — 화면 비율이 바뀌면 선 굵기와 각도가 왜곡되고, `%`는 화면 이동/확대와 안 맞는다. 노드 레이어와 SVG 레이어에 **같은 `transform` 하나**를 걸고, 선에 `vector-effect: non-scaling-stroke` 를 준다.

---

## 저장 (§8)

**파일이 원본이고, 브라우저 저장은 작업 중 임시 보관이다.** 매 작업은 파일을 여는 것으로 시작하고 파일로 내보내며 끝난다.

- 열면 **묻는다** — `지난 작업 이어하기` / `파일 열기`. 조용한 복원은 하지 않는다
- 화면 구석에 `마지막 내보내기 이후 N개 변경` 을 상시 표시한다. **내보내기가 유일한 영구 기록이다**
- **`1개`의 단위는 「되돌리기 한 번으로 되돌아가는 덩어리」다.** 드래그는 놓았을 때 1개. 캐릭터 삭제와 딸려 지워진 관계 4줄도 1개. `localStorage` 쓰기 시점도 같다
- 내보내기 파일명에 날짜와 시각을 박는다 — `charmap-20260726-1840.json`. `charmap.json` 으로 고정하면 파일 앱에 `charmap (1).json` 이 쌓이고 어느 게 최신인지 알 수 없다
- 앱은 파일을 읽을 때 **이름을 보지 않고** 내용의 `format`·`version`·`exportedAt` 만 본다

---

## 개발

```bash
python -m http.server 8000
```

- 앱: `localhost:8000/`
- 파서 테스트: `localhost:8000/tests/parse.test.html`
- node가 설치돼 있으면: `node tests/parse.test.js`

배포 주소와 로컬 주소는 **저장 공간이 다르다.** 개발 중 작업은 배포본과 별개로 쌓인다.

**아이패드는 입구를 하나로 통일한다** (§1) — 홈화면에 추가한 앱은 사파리와 별개의 저장 공간을 쓴다. 같은 주소인데도 사파리에서 하던 작업이 홈화면 아이콘에서는 안 보인다. 홈화면에 추가한 다음부터는 사파리로 그 주소를 열지 않는다.
